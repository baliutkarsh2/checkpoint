"""Bundled, zero-setup demo assets for `checkpoint demo`.

Shipped inside the wheel (unlike the repo-only `examples/` and `scenarios/`) so
the demo works from a bare `pip install checkpoint-agents` with no git checkout.
"""

__pycache__/
*.pyc
.venv/
.venv-new/
*.egg-info/
build/
dist/
.trace.json
.checkpoint/
.gstack/

# Secrets — never commit real credentials. .env.example is the only tracked env file.
.env
.env.*
!.env.example

# macOS Finder metadata
.DS_Store

# Syncthing / Nextcloud conflict files
*.sync-conflict-*

# Dashboard SPA dev — node_modules and any local Vite/tsc build artifacts.
# The committed bundle lives at checkpoint/dashboard/static/ (shipped in the wheel).
checkpoint/dashboard/web/node_modules/
checkpoint/dashboard/web/dist/

# Local pytest output dumps from this session
pytest-out.txt
dash-test.txt
dash2.txt
sse-test.txt
fullsuite.txt


# Playwright (dashboard render smoke test) artifacts
checkpoint/dashboard/web/test-results/
checkpoint/dashboard/web/playwright-report/
checkpoint/dashboard/web/blob-report/
checkpoint/dashboard/web/playwright/.cache/

# Coverage data and the local project cache.
.coverage
.coverage.*

# Scratch worktrees created by local tooling.
.claude/worktrees/
